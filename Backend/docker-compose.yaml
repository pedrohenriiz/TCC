version: '3.9'
services:
  api:
    build: .
    container_name: backend
    ports:
      - '8000:8000'
    volumes:
      - ./app:/app # Permite hot reload enquanto você desenvolve
      - ./data:/app/data
      - ./projects:/app/projects
